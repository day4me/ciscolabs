#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main()
{
	vector <string> ip_table{ "127.0.0.1", "192.168.0.1", "192.168.1.100", "255.255.255.255" };
	int n = 0;

	cin >> n;
	if (n >= 5)
	{
		cout << "Too big - value must be smaller than or equal to vector size." << endl;
		system("pause");
		exit(0);
	}

	vector <string> ip_copy(ip_table.size());
	
	for (int i = 0; i < n; ++i) {
		ip_copy[i] = ip_table[i];
	}
	for (string ip : ip_copy)
		cout << ip << " ";

	system("pause");
	return 0;
}
